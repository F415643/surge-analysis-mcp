# 股票暴涨分析 MCP 服务依赖包
# Stock Surge Analysis MCP Service Dependencies

# 核心 MCP 框架
mcp>=1.0.0
fastmcp>=0.2.0

# 数据获取和处理
akshare>=1.9.0
pandas>=1.3.0
numpy>=1.21.0

# 数据验证和类型检查
pydantic>=2.0.0
typing-extensions>=4.0.0

# 日期时间处理
python-dateutil>=2.8.0

# 异步支持
aiohttp>=3.8.0
asyncio-throttle>=1.0.0

# 可选依赖 - 用于扩展功能
# yfinance>=0.2.0  # Yahoo Finance 数据源（可选）
# matplotlib>=3.5.0  # 图表绘制（可选）
# plotly>=5.0.0  # 交互式图表（可选）